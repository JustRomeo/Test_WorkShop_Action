name: Tests
on:
  push:
    branches:
      - master
      - Dev
  pull_request:
    branches:
      - master
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - id: step1
      run: echo "Welcome to the Github Actions workshop !"
